let n = 10000000
let t0 = time_ms()
let total = 0
let i = 0
while i < n:
    total = total + (i * 3) - (i / 2)
    i = i + 1
let elapsed = time_ms() - t0
println("[Aether While] Total:", total)
println("[Aether While] Time:", elapsed, "ms")
