let n = 1000000
let t0 = time()
let total = 0
let i = 0
while i < n:
    total = total + (i * 3) - (i / 2)
    i = i + 1
let t1 = time()
println("Loop Result:", total)
println("Time:", (t1 - t0) * 1000.0, "ms")
