name: Deploy Documentation Website

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Prepare Documentation
        run: |
          mkdir -p public
          # Deploy full website assets (Landing, Docs portal, Playground, Quantum Studio)
          cp -r website/* public/
          cp README.md public/
          cp -r logo public/logo 2>/dev/null || true
          # Sync book & markdown docs into public/docs/ cleanly without directory nesting
          cp docs/*.md public/docs/ 2>/dev/null || true
          cp -r docs/book public/docs/book 2>/dev/null || true

      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './public'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
